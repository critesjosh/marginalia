# The App runtime reads this. Bundle variables reach it as env vars declared on
# the app resource; nothing here names a workspace, a credential, or a host.
command: ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]
