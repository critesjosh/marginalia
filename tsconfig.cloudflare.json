{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.cloudflare.tsbuildinfo",
    "target": "ES2022",
    "lib": ["ES2022", "WebWorker"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "allowImportingTsExtensions": true,
    "noEmit": true,
    "strict": true,
    "skipLibCheck": true,
    "types": ["@cloudflare/workers-types"]
  },
  // Tests run under Vitest with Node built-ins available; this project exists
  // to typecheck what is actually deployed to the Workers runtime.
  "exclude": ["**/*.test.ts"],
  "include": [
    "workers/app/src/**/*.ts",
    "workers/app/worker-configuration.d.ts",
    "shared/**/*.ts"
  ]
}
